<Window x:Class="PluginCoverShuffle.UI.CoverShuffleManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:PluginCoverShuffle.UI"
        Title="Cover Shuffle Manager" Height="520" Width="640" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ui:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <DockPanel Margin="10">
        <TextBox DockPanel.Dock="Top" x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,8" />

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Enable" Width="70" Click="EnableButton_Click" />
            <Button Content="Disable" Width="70" Margin="6,0,0,0" Click="DisableButton_Click" />
            <TextBlock Text="Interval (hours):" VerticalAlignment="Center" Margin="16,0,4,0" />
            <TextBox x:Name="IntervalBox" Width="50" Text="24" />
            <Button Content="Set Interval" Margin="6,0,0,0" Click="SetIntervalButton_Click" />
            <Button Content="Export..." Margin="16,0,0,0" Click="ExportButton_Click" />
            <Button Content="Import..." Margin="6,0,0,0" Click="ImportButton_Click" />
            <Button Content="Maintenance..." Margin="16,0,0,0" Click="MaintenanceButton_Click" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Margin="0,8,0,0" Opacity="0.8" />

        <TextBlock Text="No games are managed by Cover Shuffle yet. Enable Cover Shuffle or add a cover to a game to see it here."
                   TextWrapping="Wrap" Opacity="0.7" Margin="0,20,0,0"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}" />

        <DataGrid ItemsSource="{Binding Games}" AutoGenerateColumns="False" CanUserAddRows="False"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected}" Width="30" />
                <DataGridTextColumn Header="Game" Binding="{Binding GameName}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn Header="Covers" Binding="{Binding CoverCount}" Width="70" IsReadOnly="True" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
